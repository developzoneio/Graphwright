---
key: GW-26
url: https://trminhtrong.atlassian.net/browse/GW-26
type: Story
status: To Do
priority: Medium
parent_epic: GW-1
reporter: Trong Tran
assignee: Trong Tran
created: 2026-06-26T14:16:35+0700
updated: 2026-06-26T14:17:38+0700
components: none
labels: none
linked_issues: blocks GW-4
fetched: 2026-06-26
---

# GW-26 — Scaffold solution structure (Clean Architecture)

## Summary

Scaffold solution structure (Clean Architecture).

## Description

Create the `Graphwright.sln` and four projects with strict inward-only dependencies, so all
subsequent Month 1 work has a home. Prerequisite for GW-4 through GW-12 (each presumes the
projects already exist).

**Projects (innermost -> outermost):** `Graphwright.Domain`, `Graphwright.Application`,
`Graphwright.Infrastructure`, `Graphwright.McpServer`.

## Acceptance criteria

- `Graphwright.sln` with four projects, all `net8.0`, `<Nullable>enable</Nullable>`,
  `LangVersion latest`.
- Project references — dependency rule points inward:
  - `Graphwright.Domain` -> (no project references)
  - `Graphwright.Application` -> Domain
  - `Graphwright.Infrastructure` -> Application
  - `Graphwright.McpServer` -> Application + Infrastructure (composition root / DI wiring)
- Package placement honors the boundary: Roslyn (`Microsoft.CodeAnalysis.CSharp.Workspaces`,
  `Microsoft.Build.Locator`) + `sqlite-vec` in **Infrastructure**; MediatR + DTOs/contracts in
  **Application**; MCP host bootstrap in **McpServer**; **Domain** stays dependency-free.
- Root `Directory.Build.props` (shared TFM, nullable, analyzers) and `.editorconfig` encoding
  the `== false` / `== true` negation style + naming rules.
- `dotnet build` succeeds; no project references an outer layer.

## Relationships

- **Parent epic**: GW-1 — "Month 1: Drop-in MVP (GitNexus Replacement)".
- **Blocks**: GW-4 — "MCP server scaffold — transport, tool registry, error envelope"
  (GW-4 is blocked by GW-26). Snapshot at `related/GW-4.md`.
- **Linked Confluence pages**: none (`getJiraIssueRemoteIssueLinks` returned empty).
- **Subtasks**: none.

## Comments

None.

---

_Snapshot of remote ticket state as of 2026-06-26 (fetched via Atlassian MCP). Re-fetch overwrites this file._
